# Supabase クリーンアップマニュアル

## 概要
現在、`nodes`、`logs`、`projects` などの必要なテーブル以外の「使わないDB（テーブル等）」が存在する可能性があるとのことですが、
エージェント側からの外部接続（MCP連携）が利用できないため、ユーザーの皆様に手動での削除をお願いしております。

## 不要なテーブルやカラムの削除手順

1. **Supabase ダッシュボードへのアクセス**
   - [Supabase](https://supabase.com/dashboard) にログインし、該当のプロジェクトを選択します。

2. **Table Editor（テーブル一覧）の確認**
   - 左側のメニューから `Table Editor`（表のアイコン）をクリックします。
   - プロジェクト内のすべてのテーブルが一覧表示されます。
   - 当アプリで現在使用しているテーブルは以下の **3つ** のみです：
     - `projects`
     - `nodes`
     - `logs`

3. **不要なテーブルの削除**
   - 上記3つ以外のテーブル（例: 過去のテスト用テーブル、未使用の機能のテーブル等）が存在する場合、対象のテーブル名をクリックします。
   - 右上の設定アイコン（歯車）または `Drop table` メニューから、テーブルを削除（Drop）してください。
   - ※削除すると復元できませんので、本当に不要なテーブルか十分にご確認ください。

4. **不要なカラムの整理（必要な場合）**
   - 各テーブル (`projects`, `nodes`, `logs`) 内に、過去の仕様変更で使わなくなったカラムがある場合も削除可能です。
   - 例えば、`nodes` テーブルには `id`, `parent_id`, `project_id`, `label`, `summary`, `created_at` などが存在します。（※ `summary` は今回のアップデートで追加されたものです）
   - 未使用のカラムを削除する場合は、対象テーブルを開き、該当カラムのメニューから `Drop column` を選択します。

## 注意事項
- 削除操作は元に戻すことができません。不安な場合は、操作前に `Database` -> `Backups` からバックアップを作成することを推奨します。
- 環境変数 `NEXT_PUBLIC_SUPABASE_URL` と `NEXT_PUBLIC_SUPABASE_ANON_KEY` が正しいプロジェクトを向いていることを再度ご確認ください。
